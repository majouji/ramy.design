- name: package-update
  uses: taichi/actions-package-update@master
  env:
    AUTHOR_EMAIL: ramy@majouji.me
    AUTHOR_NAME: Ramy Majouji
    EXECUTE: "true"
    GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    UPDATE_COMMAND: yarn
  with:
    args: upgrade --latest